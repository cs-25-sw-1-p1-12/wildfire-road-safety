#include "library.h"
#include <stdio.h>


float GetFireDstToRoad(RoadData road, FireData fire) {
    float dst = 10000000;
    //Calculate distance

    if (dst >= 10000000) return -1;
    return dst;
}